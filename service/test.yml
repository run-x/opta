meta:
  parent: "../env/ankur-test.yml"
  name: test
  variables:
    tag: ""
modules:
  - app:
      type: k8s-service
      target_port: 5000
      tag: "{tag}"
      public_uri: "dummy.{parent[domain]}"
      secrets:
        - DATADOG_KEY
        - STRIPE_KEY
      env_vars:
        - name: APPENV
          value: prod
