{
  "$id": "https://app.runx.dev/schemas/azure-redis",
  "description": "Creates a Redis instance",
  "type": "object",
  "properties": {
    "sku_name": {
      "enum": ["Basic", "Standard", "Premium"],
      "description": "The SKU of the Redis instance"
    },
    "family": {
      "enum": ["C", "P"],
      "description": "The family/pricing group of the Redis instance"
    },
    "capacity": {
      "type": "integer",
      "description": "The [size](https://azure.microsoft.com/en-us/pricing/details/cache/) (see the numbers following the C or P) of the Redis cache to deploy.",
      "minimum": 0,
      "default": 2
    }
  }
}
