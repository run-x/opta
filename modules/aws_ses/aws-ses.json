{
  "$id": "https://app.runx.dev/modules/aws-ses",
  "description": "Sets up AWS SES for sending domains via your root domain",
  "type": "object",
  "properties": {
    "mail_from_prefix": {
      "type": "string",
      "description": "Subdomain to use with root domain.",
      "default": "mail"
    },
    "type": {
      "description": "The name of this module",
      "enum": [
        "aws-ses"
      ]
    }
  },
  "required": [
    "type"
  ],
  "opta_metadata": {
    "module_type": "env",
    "clouds": [
      "aws"
    ]
  }
}