environments:
  - name: staging
    # this should work by using the .yaml file
    path: "../dummy_data/dummy_config_parent.yml"
name: app
modules:
  - type: k8s-service
    name: app
    image: AUTO
